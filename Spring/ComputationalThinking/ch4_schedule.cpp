#include <iostream>
using namespace std;

// [ Scheduling ] -> minimize total time
// Greedy Approach로 정확한 해가 구해짐
// Sort the jobs by service time in non-decreasing order;
// and do it !! 
/*
while ( the instance is not solved  ) {
    schedule the next job; // 선택해도 되는지 ( Feasibility Check ) 확인 X 

    // program solve 조건 
    if ( there are no more jobs ) {
        the instance is solved;
    }
}
*/
